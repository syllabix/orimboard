apiVersion: skaffold/v2beta29
kind: Config
metadata:
  name: client
build:
  artifacts:
  - image: gcr.io/theta-computing-366519/client
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - k8s/*.yml
